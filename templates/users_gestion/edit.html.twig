{% extends 'admin.html.twig' %}
{% block edit %}

	<h1>{{ user.email }}</h1>
	<h2>{{ user.surname }}</h2>


	
<div class="flexBtn">
	{{ include('users_gestion/_form.html.twig', {'button_label': 'Mise à jour'}) }}

	<a href="{{ path('app_users_gestion_index') }}" class="btn btn-primary btn-icon-split btnE">back to list</a>

	{{ include('users_gestion/_delete_form.html.twig') }}

</div>
{% endblock %}
