{% extends 'admin.html.twig' %}
{% block showavis %}
{% block title %}Avis{% endblock %}

    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ avi.createdAt ? avi.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ avi.text }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ avi.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_gestion_index') }}">back to list</a>

    <a href="{{ path('app_avis_gestion_edit', {'id': avi.id}) }}">edit</a>

    {{ include('avis_gestion/_delete_form.html.twig') }}
{% endblock %}
